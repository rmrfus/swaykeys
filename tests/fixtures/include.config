# Includes: relative path, glob, and a cycle back to ourselves.
set $mod Mod4

include nested.config
include include.d/*.config
# Cycling back must terminate rather than blow the stack; the realpath dedup
# is what stops it.
include include.config

bindsym $mod+z exec from-root
